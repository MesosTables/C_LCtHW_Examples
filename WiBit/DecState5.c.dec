#include <stdio.h>

// loop keywords "break"
int main ()
{

int i = -1;

while (i<10)
	{
	i++;
	if (( i >1) && (i % 2 == 0))
	break;
	//this will break the loop if i is an even number greater than 1
	printf ("%d\n\n", i);
	}
	
// using a Label to create a loop
int x = 0;

START:

printf("%d\n", x);
if (x <2)
	{
	x++;
	goto START; // this will move the code back to line 21
	}
	
return 0;
}